# Acceptance Matrix

SS-001 is supported by the architecture contract in `docs/desktop-app-project-plan.md`. Electron is explicitly the first implementation path; main, preload, renderer, and shared TypeScript responsibilities are separated; IPC is narrow and credentials are excluded from project data; local files and offline editing have assigned owners; macOS, Windows, and Linux share the architecture; and installed launch is defined as bundled local assets with no loopback server, Python runtime, terminal, CLI, or manually opened browser. The web smoke row now matches the immutable oracle: `tests/smoke_manifest.json` invokes `scripts/start_web_smoke.py` and `scripts/check_web_smoke.py`, labels itself `existing-drawdb-web-baseline`, and expressly does not claim Electron implementation evidence. Executable Electron shell and packaged-launch proof remain correctly assigned to SS-002 and SS-003.

# Checks Run

From `/home/lee/projects/drawdb`, `npm run test` exited 0 with 2 test files passing, 0 failures, and 0 skips; `npm run lint` exited 0 with zero warnings under `--max-warnings 0`; and `npm run build` exited 0 after Vite transformed 2,648 modules and produced `dist`. Every deterministic command in the SS-001 Acceptance Checks table was then run and exited 0, including the reconciled pinned-manifest assertion. `python3 scripts/start_web_smoke.py` was also attempted, but the restricted runner rejected binding `127.0.0.1:4173` with `listen EPERM`; therefore the network smoke check could not be executed here. That environment limitation does not weaken the passing manifest-contract check or claim Electron proof. The build reported existing advisory warnings about dependency `eval` use and chunk size, but neither warning failed a required gate.

# Preservation Evidence

The required test gate exercised `src/erdTool/projectAdapter.test.js` and `src/erdTool/elkLayout.test.js`: canonical project conversion, credential-field rejection, Snowflake identifier and DDL behavior, and non-mutating ELK layout remained green. The lint gate covered repository JavaScript and JSX without warnings, while the production web build completed from the existing React/Vite feature tree. No application source, package dependency, npm script, Electron runtime, or pinned smoke-oracle file was changed. The exact content files changed for this acceptance repair are `docs/desktop-app-project-plan.md`, `docs/desktop-app-sprint-plan.md`, and `artifacts/ss-001-acceptance-report.md`. In particular, `tests/smoke_manifest.json`, `scripts/start_web_smoke.py`, and `scripts/check_web_smoke.py` were inspected but left unmodified.

# Closure Decision

SS-001 is complete as of 2026-07-14 because each requirement in its architecture-document acceptance slice has both explicit contract text and passing deterministic evidence, and all three preservation gates pass. This closure does not assert that an Electron shell, native bridge, installer, or packaged no-server launch already exists; those are later implementation slices. The current SS-001 status is `complete`, and the next recommended sprint item is SS-002, “Scaffold Electron Without Breaking Web Build.” No blocker requires Lee. The only observed execution constraint was the managed runner’s refusal to open a loopback listener for the optional web smoke attempt; a user smoke can run that immutable harness in an environment that permits localhost binding, but it is not required to reopen SS-001.
